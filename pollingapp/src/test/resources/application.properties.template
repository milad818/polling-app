# TEST CONFIGURATION - used automatically during test runs, never affects the main app
# INSTRUCTIONS:
# 1. Copy this file and rename it to 'application.properties' (in this same directory)
# 2. The values below are safe defaults for testing — adjust only if needed

# H2 in-memory database (no real DB connection needed)
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# JWT (dummy values safe for testing)
jwt.secret=testSecretKeyThatIsAtLeast32CharsLong!
jwt.expiration-ms=3600000
